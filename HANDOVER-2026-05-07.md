# FTC Platform — Handover Summary (2026-05-07)

## Context
This handover covers all actions, fixes, and automation completed in the May 7, 2026 session for the FTC Holistic Platform Consolidation (Phase A/B). It is intended for a new developer to continue seamlessly.

---

## 1. Phase A/B Checklist Progress
- **A1:** Dead Railway services deleted (user-confirmed)
- **A2:** GH Actions queue cancelled, noisy workflows disabled, checklist updated to DONE
- **A3:** Noisy workflows disabled (job-level `if: false`)
- **A4:** Railway infra audit script created (see below)
- **A5:** Auth allowlist audit completed, typo fixed, findings documented
- **Phase B:** All 10 agent-ready issues seeded (B1-B10)

---

## 2. Label Bootstrap & Issue Seeding
- **Problem:** GitHub Actions workflow for label sync failed (private repo, GITHUB_TOKEN checkout permission issue)
- **Solution:** Created `scripts/create-labels.mjs` to seed all labels via `gh api` (bypasses workflow)
- **Result:** 22/23 labels created (the 23rd, `bug`, already existed)
- **Phase B Issues:** Ran `npm run ci:seed:phase-b` — all B1-B10 issues created with correct labels

---

## 3. INBOX Flush & Security Bug Filing
- **Problem:** INBOX flush script failed on CRLF line endings
- **Solution:** Fixed `scripts/flush-inbox.mjs` to normalize line endings
- **Result:** Ran `npm run inbox:flush` — SayWetin hardcoded admin password bug filed as `[Bug]` issue

---

## 4. Railway Infra Inventory Automation
- **Created:** `docs/infra-inventory.md` (append-only infra snapshots)
- **Created:** `scripts/railway-audit.mjs` (captures `railway whoami` + `railway list` for current account)
- **How to use:**
  1. `railway logout`
  2. `railway login` (sign in as target account)
  3. `node scripts/railway-audit.mjs --label "enchanting account"`
- **Next:** For each project, run `railway link` + `railway status` and document services/domains. Remap `api.saywetin.app` DNS in Railway dashboard if needed.

---

## 5. PeacePad Typecheck & Build Fixes
- **Problem:** Vite build warning: `@capacitor/core` dynamically + statically imported (no chunk split)
- **Solution:** Refactored `client/src/App.tsx` and `client/src/main.tsx` to use static imports only
- **Result:** Typecheck and build pass, warning gone. Only minor warnings remain (chunk size, AccessibilityAnnouncer dynamic import).

---

## 6. PeacePad Session Restore Hotfix
- **Symptom:** PeacePad showed `Session restore needs attention`; earlier client copy exposed raw DB text such as `relation "sessions" does not exist`.
- **Client fix:** Error copy is sanitized in `prep-chat.tsx` and `onboarding.tsx`; committed as `cd7f9ec6 fix(peacepad): sanitize session restore errors in client`.
- **DB fix:** Supabase SQL Editor was used to run the existing idempotent migration `APPS/peacepad/migrations/hotfix_sessions.sql`.
- **DB verification:** `peacepad.sessions` exists with row count `0`. Confirm `public.sessions` is visible with:
  ```sql
  select
    to_regclass('public.sessions') as public_sessions,
    to_regclass('peacepad.sessions') as peacepad_sessions;
  ```
- **Runtime env:** Railway `DATABASE_URL` now points to the Supabase shared pooler host shape; `SESSION_SCHEMA` is missing, so `public.sessions` must exist.
- **Redeploy blocker:** Railway service `@ftc/peacepad` is `Online - Deploy failed`; manual redeploy was blocked by Railway free-tier peak-hour rules.
- **Scheduled redeploy:** Windows task `PeacePad Railway Redeploy 2026-05-07` is scheduled for 2026-05-07 8:05 PM ET to run `railway redeploy --service '@ftc/peacepad' --yes`.
- **Validation:** `npm run check` and `npm run build` pass from `APPS/peacepad`; warnings are non-blocking.
- **Status:** QA VERIFY after redeploy and user retest. GO only after `Retry session check` no longer shows the session error and compose/no-login still works.

---

## 7. Security Actions
- **SayWetin:** Hardcoded admin password bug filed, action required: migrate to env var, rotate, scrub git history
- **PAT Leak:** User-pasted classic PAT ([REDACTED_GITHUB_PAT_EXPOSED_IN_CHAT_ROTATE_REQUIRED]) must be revoked at https://github.com/settings/tokens

---

## 8. Outstanding Actions (for next developer)
- [ ] **Railway enchanting audit:** Log in as `enchanting` account, run audit script, document all live/dead services, remap `api.saywetin.app` DNS if needed
- [ ] **SayWetin bug:** Migrate hardcoded admin password to env var, rotate, scrub git history
- [ ] **PAT revocation:** Confirm user has revoked the leaked PAT
- [ ] **PeacePad session restore:** Confirm `public.sessions` and `peacepad.sessions`, wait for scheduled Railway redeploy, then retest `Retry session check`
- [ ] **PeacePad bundle:** Consider splitting main chunk if strict bundle gating is enabled; minor Vite warnings remain
- [ ] **INBOX:** Continue to use `npm run inbox:flush` to file new bugs/tasks

---

## 9. Key Files Created/Modified
- `.github/labels.yml` — label registry
- `.github/workflows/bootstrap-labels.yml` — label sync workflow (failed, replaced by script)
- `scripts/create-labels.mjs` — label seeder (new)
- `scripts/flush-inbox.mjs` — INBOX flush (CRLF fix)
- `scripts/railway-audit.mjs` — Railway infra snapshot (new)
- `docs/infra-inventory.md` — Infra audit log (new)
- `docs/bleed-stop-checklist.md` — Checklist (A2 marked DONE, findings updated)
- `APPS/peacepad/client/src/App.tsx`, `main.tsx` — Capacitor import fix

---

## 10. How to Continue
- PeacePad session files to know:
  - `APPS/peacepad/client/src/pages/prep-chat.tsx`, `onboarding.tsx` - session error sanitization
  - `APPS/peacepad/migrations/hotfix_sessions.sql` - idempotent sessions table migration used in Supabase SQL Editor
- Use the scripts in `scripts/` for automation (labels, infra, inbox)
- Always check `docs/bleed-stop-checklist.md` for current status
- For infra, always run `railway-audit.mjs` after switching accounts
- For bugs, file via INBOX or GitHub Issues
- For build warnings, check Vite output and bundle report

---

## Questions?
Contact previous owner or check commit history for rationale on any change.
