name: Client Project Build Trigger

on:
  repository_dispatch:
    types: [client_approved_scope]
  workflow_dispatch: {}

jobs:
  disabled:
    name: Disabled scaffold trigger
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled state
        run: |
          echo "Client project scaffolding is disabled by repo policy."
          echo "Reason: bleed-stop guard added on 2026-04-30."
          echo "Re-enable only after client intake automation has an owner, tests, and approval gates."
