# Cloudflare Pages _redirects
# Host-based brand routing is handled in functions/_middleware.ts.
# Keep this file path-only because Cloudflare Pages ignores host-prefixed rules.

# Internal path normalization (host-agnostic)
/garden-cleaners/ /garden-cleaners 301
/og-trades-academy/ /og-trades-academy 301
