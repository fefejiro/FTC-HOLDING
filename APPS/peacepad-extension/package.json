{
  "name": "@ftc/peacepad-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@ftc/peacepad-sdk": "0.1.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "@types/node": "^20.16.11",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3",
    "vitest": "^4.0.18"
  }
}
