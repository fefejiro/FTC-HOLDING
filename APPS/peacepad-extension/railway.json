{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "node -e \"console.log('peacepad-extension is a Chrome extension build artifact — no server to run'); process.exit(0)\"",
    "restartPolicyType": "NEVER"
  }
}
