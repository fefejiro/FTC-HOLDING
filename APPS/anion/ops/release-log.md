# Anion Release Log

| Date | Version | Change | Validation |
|---|---|---|---|
| 2026-06-17 | callback and Phase 1 evidence hardening | Added server-readable `token_hash` callback support for Supabase-generated evidence links, hardened the Phase 1 evidence runner to use that callback path, provisioned a three-account Google-auth role fixture, and made lesson Rejoin/Retry immediately reset visible state to `connecting`. | Verified: `npm --prefix APPS/anion run check` passed; `npm --prefix APPS/anion test` passed 30/30; `npm --prefix APPS/anion run build:worker` and `deploy:worker` succeeded with Worker Version ID `36eca01c-c5a6-425e-8b81-8aeebc172c0b`; strict `verify:prod` passed 7/7. Evidence report `test-results/phase1-call-2026-06-17-service-role-smoke-final/phase1-call-evidence.md` passes role dashboards, booking visibility, parent denial, and tutor/student Daily token issuance, but video connected/rejoin proof still fails because the lesson remains in the "Session ended / Rejoin lesson" state during automation. |
| 2026-06-17 | auth provisioning fix | Fixed the Google OAuth callback gap where Supabase Auth users could exist without Anion `profiles`, causing dashboard access to appear like a failed sign-in. `/auth/callback` now provisions a default profile, parent role, and parent row after successful OAuth; existing `fejiro.efiuvwere@gmail.com` and `peacepad@peacepad.ca` users were backfilled. Added visible sign-out in the authenticated header and surfaced OAuth errors from the browser helper. | Verified: `npm --prefix APPS/anion run check` passed; `npm --prefix APPS/anion test` passed 29/29; production DB check shows both accounts have profile, parent role, and parent row; `npm --prefix APPS/anion run build:worker` and `deploy:worker` succeeded with Worker Version ID `475abf62-81ae-4460-8ce5-70f658521ade`; strict `verify:prod` passed 7/7; Google handoff reaches Google Accounts. `prod:doctor` remains correctly blocked on authenticated role video evidence, whiteboard production evidence, and Stripe subscription-state evidence. |
| 2026-06-16 | 0.2.20 | Applied the recurring class plan and whiteboard schemas to production Supabase with `supabase db query --linked`, deployed PR1/PR2/PR3 plus runtime status alignment to Cloudflare Worker version `46b60191-b129-4165-a6d4-c4260199e906`, and added `whiteboard_production_evidence_pending` to `/api/status`. | Verified: production schema checks confirmed `class_plans`, `whiteboard_events`, and recurring booking columns; `npm --prefix APPS/anion test` passed 29/29; `npm --prefix APPS/anion run check`, `npm --prefix APPS/anion run build`, and `npm --prefix APPS/anion run build:worker` passed; `npm --prefix APPS/anion run deploy:worker` succeeded; `verify:prod` passed 7/7. `prod:doctor` remains blocked on authenticated video evidence, whiteboard production evidence, and Stripe subscription-state evidence. |
| 2026-06-16 | realtime whiteboard MVP PR3 | Added a booking-scoped `whiteboard_events` Supabase migration and a lesson-room `WhiteboardCanvas` with pen, eraser, clear board, pointer-event input for mouse/touch/stylus, persisted stroke replay, and Supabase realtime insert sync. The existing text timeline remains as secondary lesson notes. | Verified locally: `npm --prefix APPS/anion test` passed 29/29, `npm --prefix APPS/anion run build` passed, and `npm --prefix APPS/anion run check` passed after build regenerated `.next/types`. No deploy performed; production migration, realtime sync proof, and authenticated tutor/student reload-restore evidence remain required. |
| 2026-06-16 | one-on-one classroom hardening PR2 | Hardened the Daily room token path for one-on-one lessons: assigned tutor/student authorization remains enforced, join tokens are blocked until 10 minutes before the scheduled start, token issuance closes at class end plus 15 minutes, and newly created Daily rooms are capped to two participants. Added focused route and join-window tests. | Verified locally: `npm --prefix APPS/anion test` passed 26/26, `npm --prefix APPS/anion run check` passed after build regenerated `.next/types`, and `npm --prefix APPS/anion run build` passed. No deploy performed; authenticated production parent/tutor/student call evidence remains required before Phase 1 is green. |
| 2026-06-16 | recurring class plan PR1 | Added the recurring class plan engine using the existing `bookings` table as generated class sessions. Admins can create a plan once, generating the next 8 weeks of accepted recurring bookings with `Africa/Lagos` timezone, 50-minute default class duration, 10-minute buffer, parent/student/tutor relationship validation, and tutor/student overlap checks. Added the Supabase migration for `class_plans`, recurring booking metadata, admin RLS, and scheduler tests. | Verified locally: `npm --prefix APPS/anion test` passed 22/22, `npm --prefix APPS/anion run check` passed, and `npm --prefix APPS/anion run build` passed. No deploy performed; production Supabase migration still needs to be applied and verified. |
| 2026-06-15 | role routing and parent booking QA fix | Fixed multi-role account routing so explicit pages prefer their required role and `/dashboard` prioritizes admin/tutor/student before parent. Added a visible parent dashboard empty-state when no student is linked, disabled the booking submit button in that state, and linked the existing production Phase 1 Student QA row to Mike's parent/admin account so the live parent booking form can be tested from the account shown in feedback. | Verified: `npm run check`, `npm test` 19/19, `npm run test:e2e` 17 passed/5 intentional skips, `npm run test:local-video` 3/3, `npm run build`, and `npm run build:worker` passed; deployed Worker Version ID `4c42883b-f9f9-467d-ab30-43ada09ff6eb`; production `verify-prod --check-daily-room` passed 7/7; Playwright production login smoke had Google visible, magic-link absent, and no console errors; production parent-student links now include `mike.fejiro` -> `Phase 1 Student`. `prod:doctor` remains blocked only on Stripe subscription-state evidence and authenticated video evidence. |
| 2026-06-15 | login manifest icon fix | Replaced the missing manifest icon references with a static `/icon.svg` asset and compatibility responses for `/icon-192.png` and `/icon-512.png`, removing the production login console 404 reported for `icon-192.png`. | Verified: `npm run check`, `npm run build`, and `npm run build:worker` passed; deployed Worker Version ID `87b142d0-c004-410c-a1da-984d746eaed1`; live `/icon.svg`, `/icon-192.png`, `/icon-512.png`, and `/manifest.webmanifest` return HTTP 200; Playwright login pass had no console warnings/errors from `/login`; Google OAuth handoff decodes to `redirect_to=https://anion.unalabs.cloud/auth/callback`; `verify-prod --check-daily-room` passed 7/7. `prod:doctor` remains blocked only on Stripe subscription-state evidence and authenticated video evidence. |
| 2026-06-15 | ftc-site edge redirect | Deployed a `functions/_middleware.ts` edge redirect in the real `ftc-site-pages` Cloudflare Pages project so accidental Supabase callbacks to `https://unalabs.cloud/?code=...` return `307` to `https://anion.unalabs.cloud/auth/callback` before rendering the Una Labs page. This is a mitigation for the observed Google redirect issue, not a substitute for directly confirming Supabase Auth URL configuration. | Verified: `npm run build` in `APPS/ftc-site` passed; deployed to `ftc-site-pages` production branch; direct request to `https://unalabs.cloud/?code=server-proof-final&state=bridge-proof` returned `HTTP 307` with `Location: https://anion.unalabs.cloud/auth/callback?...`; Playwright browser proof reached `https://anion.unalabs.cloud/login?error=auth_callback_failed`, proving the code was forwarded and failed only because the proof code was fake. `verify-prod --check-daily-room` still passes 7/7; `prod:doctor` remains blocked only on authenticated Google video evidence and Stripe subscription-state evidence. |
| 2026-06-14 | 0.2.19 | Replaced the Cloudflare Worker `SUPABASE_SERVICE_ROLE_KEY` from the Supabase CLI project service_role key, added `npm run phase1:provision-google-qa` to upsert dedicated Google QA role fixtures after first Google sign-in, and aligned `/api/status` plus ops docs so service-role is no longer a blocker. | Verified: Supabase service_role key returned HTTP 200 against production REST before upload; `npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY --name anion-web` succeeded; `npm run check`, `npm test`, `npm run build`, and `npm run build:worker` passed; deployed Worker Version ID `d37e3bc4-b83d-4e5f-9931-ab7293747b98`; production `verify-prod --check-daily-room --check-stripe-webhook` passed 7/7; `prod:doctor` now blocks only on authenticated Google video evidence and Stripe subscription-state evidence. |
| 2026-06-14 | 0.2.18 | Replaced the hosted Daily prebuilt iframe in the lesson room with an Anion first-party Daily call-object UI, preserving tutor/student join, leave, rejoin, and adding in-call background controls (Off, Soft, Strong). Hardened Phase 1 evidence to require visible video plus background switching and added a manual Google-auth evidence path for real role sessions when service-role secrets are unavailable. Runtime `/api/status` now reports custom call UI ready with authenticated video evidence pending instead of the old Daily hosted-UI CDN blocker. | Verified: `npm run check`, `npm test` 16/16, `npm run test:local-video` 3/3, `npm run test:e2e` 17 passed/5 skipped, `npm run build`, `npm run build:worker`; deployed Worker Version ID `c8cdb241-5c0f-46f1-aabb-aef3480f63eb`; production `verify-prod --check-daily-room` passed 7/7; deployed lesson chunk has `createCallObject`, custom room UI, and no `createFrame`. Remaining blockers: invalid Worker `SUPABASE_SERVICE_ROLE_KEY`, Stripe subscription-state evidence, authenticated video evidence, legal signoff. |
| 2026-06-09 | 0.2.17 | Repaired production Phase 1 data/RLS path: added non-recursive role helper policies, repaired parent/tutor/student fixture rows and accepted booking, refreshed Daily secrets, and made Daily room creation retry without cloud recording when the current Daily plan rejects recording. Runtime `/api/status` now reports current blockers instead of stale fixture/auth blockers. | Verified: `npm run check`, `npm test` 16/16, `npm run build:worker`, deployed Worker Version ID `b4777f0d-229c-4fc1-a539-0d86aedd5fb0`; production password evidence passes dashboards, parent `403 LESSON_ACCESS_DENIED`, and tutor/student Daily token issuance. Remaining blockers: invalid Worker `SUPABASE_SERVICE_ROLE_KEY` for Stripe/webhook sync, `https://c.daily.co` unreachable from evidence machine for iframe join, legal signoff. |
| 2026-06-09 | 0.2.16 | Made production status fail-closed on invalid service-role and missing Phase 1 domain fixture rows; hardened parent/tutor/student dashboards so incomplete domain rows do not 500; updated production doctor to respect runtime blocker codes. | Verified: `npm run check`, `npm test`, `npm run test:e2e`, `npm run build:worker`, strict `verify:prod` passed 7/7; `prod:doctor` intentionally BLOCKED on `supabase_service_role_invalid`, `phase1_domain_fixture_missing`, auth allow-list confirmation, and confirmed credentials. Worker Version ID `fd56ef60-f89a-4b55-8ef8-9ffa3611f046` |
| 2026-06-09 | 0.2.15 | Fixed production public auth config by injecting Supabase browser config from Cloudflare Worker runtime bindings, restored Google primary plus magic-link fallback, hardened browser-bundle placeholder guard, and aligned handover docs to provider-ready/evidence-pending truth. | Verified locally: `npm run check`, `npm test`, `npm run build:worker`; production: `npm run prod:doctor` and strict `verify:prod` passed after deploy; Worker Version ID `112bee96-1038-4ec6-8316-14e037636716` |
| 2026-05-26 | 0.2.13 | Deployed Daily classroom hardening without porting PeacePad WebRTC: testable `/api/daily/room` handler, tutor/student-only call access, parent call denial, clearer join/error/left/rejoin UI, and updated production evidence docs. | Verified locally: `npm run check`, `npm test`, `npm run test:e2e`, `npm run build`; production verify passed 6/6 after deploy; Worker Version ID `e38ad0ca-012c-4a38-bdac-66258a4878d2` |
| 2026-05-21 | 0.2.12 | Deployed Anion worker to production (`anion.unalabs.cloud`) with auth redirect hardening and premium login surface updates. Production verify script now accepts CSRF-protected Daily room smoke responses in non-destructive mode. | Verified: `npm run verify:prod` passed 6/6 after deploy; Worker Version ID `e5f96805-0b41-41f9-b37e-554c7f2ea676` |
| 2026-05-21 | 0.2.11 | Unblocked production build pipeline: installed missing Tailwind runtime dependency (`@tailwindcss/node`), refreshed local Next SWC binary state, suppressed expected dynamic-server auth noise during static rendering, and removed duplicate `tsconfig` path keys. OpenNext worker artifact generation now completes successfully. | Verified: `npm run ci:check` and `npm run build:worker` in `APPS/anion` |
| 2026-05-21 | 0.2.10 | Hardened auth redirects: login now sends both magic-link and Google auth through `/auth/callback` with optional canonical domain fallback (`NEXT_PUBLIC_AUTH_REDIRECT_URL`/`NEXT_PUBLIC_SITE_URL`). Premium login page refreshed while preserving smoke selectors and explicit error/success states. | Verified: `npm run ci:check`, `npm run verify:prod`, and production worker deployment completed |
| 2026-05-20 | 0.2.9 | Logged all current blockers: Supabase Auth magic-link redirect domain incorrect (unalabs.cloud, not anion.unalabs.cloud), authenticated session handoff blocked, Daily join/leave evidence not possible, Privacy/Terms legal review pending. All status artifacts aligned to red/non-green. | Verified: blockers and evidence in ops/PHASE1-CALL-PRODUCTION-CLOSURE.md, status-summary.json, weekly-status.md, PRODUCTION-READINESS.md |
| 2026-05-20 | 0.2.8 | Deployed runtime truth-alignment update so live `/api/status` reflects blocked Phase 1 closure contract (`phase1-call-closure-failed-auth-blocked`) instead of scaffold bootstrap payload. | Verified via direct no-cache GET to `https://anion.unalabs.cloud/api/status` after fresh `build:worker` + `deploy:worker` |
| 2026-05-20 | 0.2.7 | Executed Phase 1 production call closure attempts for parent/tutor/student. Hard blocker confirmed: all documented role test credentials return `invalid_credentials` in production. Runbook evidence table and failure matrix updated; truth artifacts moved to red/non-green state. | Verified via direct Supabase auth password probes and recorded in `ops/PHASE1-CALL-PRODUCTION-CLOSURE.md` |
| 2026-05-20 | 0.2.6 | Start Phase 1 implementation: added strict call production closure runbook/evidence template; aligned AGENTS, README, ROADMAP, weekly status, and status summary to non-green governance while critical blockers remain open. | Documentation/governance update only; runtime deployment verification pending |
| 2026-05-20 | 0.2.5 | Production-readiness alignment: `/api/status` now reports production handoff state and external blockers; public legal routes `/privacy` and `/terms` added and linked from global footer; cookie consent banner added; status-summary artifact synchronized to current handoff stage. | Passed (`npm run check` and `npm run build` on 2026-05-20) |
| 2026-05-20 | 0.2.4 | Fix: Guard Supabase init in middleware when env vars are absent. `/api/health` now excluded from middleware matcher and middleware skips `createServerClient` gracefully when URL/anon key are not set. Cloudflare Worker version `61951451` deployed. | Verified â€” `https://anion.unalabs.cloud/api/health` â†’ `{"ok":true}` |
| 2026-05-19 | 0.2.3 | Premium UX pass completed across core surfaces (`globals.css`, root layout, home, login, pricing, lesson room states). Follow-up fix applied for Supabase auth type imports to restore CI type-check compatibility. | Passed (`npm run ci:check` on 2026-05-19) |
| 2026-05-06 | 0.2.2 | Security hardening baseline: API CORS allow-list, CSRF checks for state-changing routes, security headers baseline, runtime-selectable rate limiter (Cloudflare KV path + memory fallback), and focused security tests/docs | In progress |
| 2026-05-06 | 0.2.1 | M1 auth flow execution: callback route, magic-link redirect fix, seeded role profile, local unattended sign-in smoke verified; RLS policy migration authored | Partial (policy rollout pending) |
| 2026-05-05 | 0.2.0 | M0 platform realignment: Next.js App Router migration, OpenNext setup, env contract cleanup, Supabase migration scaffold, CI baseline | In progress |
| 2026-04-23 | 0.1.0 | Initial foundation scaffold | Pending |
