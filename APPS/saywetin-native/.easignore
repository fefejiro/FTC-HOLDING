# Exclude local deploy artifacts and generated symlink-heavy outputs from EAS upload
.tmp-ftc-deploy/
**/.vercel/
.agents/
.claude/

# Credential artifacts must never enter an EAS upload
credentials.json
**/credentials.json
