{
  "name": "capsigma-growth-desk",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "check": "npm run test && npm run build",
    "prod:doctor": "node scripts/prod-doctor.mjs",
    "prod:smoke": "node scripts/prod-smoke.mjs",
    "prod:test-recipients": "node scripts/prod-test-recipients.mjs",
    "sendgrid:set-secret": "node scripts/set-sendgrid-secret.mjs",
    "db:migrate:local": "npx wrangler d1 migrations apply capsigma-growth-desk --local",
    "db:migrate:remote": "npx wrangler d1 migrations apply capsigma-growth-desk --remote",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name capsigma-growth-desk --branch main --commit-dirty=true"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
