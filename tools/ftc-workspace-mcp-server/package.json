{
  "name": "@ftc/workspace-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only FTC Holding workspace, Git worktree, disk, and cache context for coding agents.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/index.js --self-test",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  }
}
